# Before All

```rhai

return devai::before_all_response(#{
    before_all: inputs,
    inputs: ["one", "two", "three", 4, "five"]
});

```

# Data 

```rhai

if input == "two" {
    return devai::action_skip("Skip the 'two' at data stage");
}

return "Data with input: '" + input + "'"

```

# Output

```rhai

if input == "five" {
    return devai::action_skip("Don't output process 'five'");
}

if input == 4 {
    return;
} else {
  return #{
    before_all_data_was: before_all,
    data: data
  }
}



```

# After All

```rhai

return "this is after all data: inputs: " + inputs + " - " + outputs.len() ;
```