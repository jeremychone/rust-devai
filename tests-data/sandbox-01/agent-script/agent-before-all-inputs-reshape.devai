# Before All

```rhai

inputs[0] = inputs[0] + "-0";
inputs[1] = inputs[1] + "-1";
inputs.push("C");
return devai::before_all_response(#{
    before_all: "some before all data",
    inputs: inputs
});

```

# Data 

```rhai

return "Data with input: '" + input + "'"

```

# Output

```rhai

return data

```