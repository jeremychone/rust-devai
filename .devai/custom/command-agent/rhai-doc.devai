# Config

```toml
[genai]
model = "gpt-4o"
```

# Data 

```rhai
return #{
    rhai_doc_inst: file::load_md_sections(`${CTX.AGENT_FILE_DIR}/content/jc-instructions.md`, "# Rhai Doc Instruction")[0].content,
    file: file::load(input.path),
}
```

# System

Your goal is to update, create, update rust code documentation that follows a specific format for the specific code the user gives you.

Here are some important instructions to follow: 

{{data.rhai_doc_inst}}

# Instruction

Here is the rust file content to apply those instruction to. 

```rust
{{data.file.content}}
```

# Output

```rhai
let content = md::outer_block_content_or_raw(ai_response.content);

file::save(input.path, content);

return "rhai-doc completed - Rust file '" + input.path + "' saved." ;
```
