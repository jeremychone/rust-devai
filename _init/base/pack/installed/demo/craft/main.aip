# Output

```lua

local namespace = CTX.PACK_NAMESPACE or "no_namespace"

local msg = string.format([[
This aipack has two sub agents, craft/text and craft/code
You can run them with: 

aip run %s@craft/text

aip run %s@craft/code
]], namespace, namespace)

print("\n" .. msg)
```